<% provide(:title, 'New Book') %> 

<%= link_to "Back", index_path, class: "back" %>

<h1>New Book</h1>

<%= render 'form' %>



<%#:html => {:multipart => true} <-- add after (@book) if using carrierwave %>